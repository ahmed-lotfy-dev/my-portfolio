/**
 * Shared form styling constants
 * Centralizes repeated form styling patterns across dashboard forms
 */

/**
 * Standard input field classes
 * Used for Input, Textarea, and other form controls
 */
export const inputClasses =
  "bg-background/50 border-white/10 focus:border-primary/50 focus:ring-primary/20 transition-all duration-300 hover:bg-background/80";

/**
 * Standard label classes
 * Used for form field labels
 */
export const labelClasses = "text-sm font-medium text-muted-foreground mb-1.5 block";

/**
 * Common form section container classes
 * Used for grouping related fields
 */
export const formSectionClasses = "space-y-6 p-6 rounded-xl bg-background/40 border border-border/50";

/**
 * Form field wrapper classes
 * Used for individual field containers
 */
export const fieldWrapperClasses = "space-y-1";

/**
 * Dialog content classes for forms
 * Used in modal/dialog-based forms
 */
export const dialogFormClasses = "max-w-[700px] overflow-hidden p-0 bg-zinc-950 border-zinc-800 shadow-2xl sm:rounded-xl";

/**
 * Submit button classes
 * Standard styling for primary submit buttons
 */
export const submitButtonClasses = "bg-primary hover:bg-primary/90 text-white px-8 py-2 rounded-md shadow-lg shadow-primary/20 transition-all hover:shadow-primary/30 hover:scale-105";
