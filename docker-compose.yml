services:
  web:
    build:
      context: .
      target: runner
    ports:
      - "3000:3000"
    restart: unless-stopped
    depends_on:
      - worker

  worker:
    build:
      context: .
      target: cron
    restart: unless-stopped
